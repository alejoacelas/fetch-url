[project]
name = "fetch-url"
revision = "1.0"
require_reason = "agents"

[providers]
developer_credentials = "onepassword://Developer-Credentials"

[profiles.default]
FIRECRAWL_API_KEY = { description = "Firecrawl API key used for blocked pages", required = true, providers = ["developer_credentials"] }
