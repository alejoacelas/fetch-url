{
  "name": "@alejoacelas/fetch-url",
  "version": "0.1.0",
  "description": "Fetch a URL as clean, source-attributed content with Firecrawl and archive fallbacks.",
  "license": "MIT",
  "repository": "github:alejoacelas/fetch-url",
  "type": "module",
  "bin": {
    "fetch-url": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x dist/cli.js",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --import tsx test/*.test.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "commander": "^14.0.3",
    "jsdom": "^28.1.0",
    "turndown": "^7.2.2"
  },
  "devDependencies": {
    "@types/jsdom": "^27.0.0",
    "@types/node": "^24.10.1",
    "@types/turndown": "^5.0.6",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
